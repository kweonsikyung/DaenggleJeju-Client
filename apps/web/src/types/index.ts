/** @file types/index.ts
 * @module types
 * @description 전역 타입 정의 (문서 전용)
 *
 * @note 이 파일은 **배럴 파일로 사용하지 않습니다.**
 *       타입은 런타임에 사라지지만, 명확성을 위해 개별 import를 권장합니다.
 * 
 * ## 디렉터리 가이드
 * - api.d.ts    : API 응답/요청 타입
 * - common.d.ts : 전역 공통 타입
 *
 * ## import 가이드
 * // import type { ApiResponse } from "@/types/api";
 */

// 문서 전용 파일입니다. 코드/exports 를 추가하지 마세요.
