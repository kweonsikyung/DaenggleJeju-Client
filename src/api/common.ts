/** @file api/common.ts
 * @module api/common
 * @description API 요청에 사용되는 공통 fetch 래퍼와 타입 정의 모음
 * 
 * ## 주요 기능
 * - HTTP 메서드별 요청 함수 (`get`, `post`, `patch`, `put`, `del`)
 * - 공통 응답 타입(`CommonResponse<T>`)
 * - Zustand에서 토큰 읽어 Authorization 헤더 자동 추가
 * - 오류 처리 표준화 (`ApiError` 클래스)
 */

export {};