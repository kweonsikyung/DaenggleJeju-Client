/** @file lib/index.ts
 * @module lib
 * @description 공용 유틸 함수, 외부 라이브러리 설정 (문서 전용)
 *
 * @note 이 파일은 **배럴 파일로 사용하지 않습니다.**
 *       각 라이브러리 설정 또는 유틸 함수는 개별 import 하세요.
 *       불필요한 번들 포함을 방지하고, 의존성 초기화를 제어하기 위함입니다.
 * 
 * ## 디렉터리 가이드
 * - swr.ts         : SWR 전역 설정/Fetcher
 *
 * ## import 가이드
 * // import { fetcher } from "@/lib/swr";
 */

// 문서 전용 파일입니다. 코드/exports 를 추가하지 마세요.
